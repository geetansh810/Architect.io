[build]
  command = "npm install"
  functions = "netlify/functions"
  [build.environment]
    SECRETS_SCAN_OMIT_KEYS = "PORT"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
